# The built native library. vendor/raygui.h is the tracked source of truth;
# lib/ is derived — `bb lib:build` recreates it.
lib/

# smoke-test screenshots (RAYGUI_APP_SHOT). raylib writes the path's BASENAME
# into the current working directory, so these land at the repo root.
shot*.png
raygui_shot.png

# clojure CLI classpath cache — created when `bb lint` falls back to running
# clj-kondo through the clojure CLI (no native clj-kondo binary installed)
.cpcache/

# clj-kondo's analysis cache. The config and the jolt.ffi/defcfn hook next to it
# ARE tracked — they're what makes `bb lint` usable.
.clj-kondo/.cache/

# clj-kondo configs clojure-lsp auto-imports from whatever's on its resolved
# classpath — unrelated to this project's own (dependency-free) deps.edn.
.clj-kondo/imports/

# clojure-lsp's analysis cache, created by `bb lsp:*`
.lsp/.cache/

# jolt's own classpath cache, created by any `joltc -M:<alias>` run
.jolt/

# local dev artifacts (not part of the example suite)
.claude/

# local tooling workspace: scratch notes and generated output, never shipped.
# Ignored here rather than relying on the .gitignore written inside that
# directory itself, which lives in an untracked tree and would stop
# protecting anything if it were removed.
.superpowers/
.DS_Store

# Generated documentation site (bb site:build). Published by CI from main;
# never committed, since a stale build in the tree is worse than none.
_site/

# A local checkout of jlt-commons/docs-engine, if you clone it here rather
# than as a sibling. CI checks it out itself.
.docs-engine/
